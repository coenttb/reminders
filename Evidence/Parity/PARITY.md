# Parity ledger — Reminders example vs iOS 27 stock Reminders

## Current state (2026-09-18, branch merged to main)

Read this first; the sections below are the dated history.

- **Matches stock (measured):** home tiles and My Lists; list detail rows, inline card and chip menus; More menu and Sort By with directions; completed header and Clear; Details sheet (large, compact cards, full-date value); New Reminder sheet with quick bar; List Info / New List; search page; swipe actions; select mode; empty state; dark mode; Dynamic Type; push/pop and sheet timings; **Today** (overdue + all-day block, Morning/Afternoon/Tonight, add circle), **Scheduled** (Overdue by day, Today, Tomorrow, five days, Rest of month, twelve months), **Completed** (Today, Previous 7 Days, months, newest first, "Completed: when"); dates `16/09/2026` / times `06:05`; Custom → Date & Time sheet; a row dragged onto a section takes its date.
- **Deliberately different / accepted:** day headers do not pin while scrolling (cost of the drag); Tags card, tag picker and the Flag row have no simulator reference (iCloud); Print and Location are not modelled; smart-group visibility and Add Group in home edit mode are not modelled.
- **Residuals:** the tapped list row is not highlighted during the push; dark lists card gray. (Closed 2026-09-18: section rule 14 pt vs 11; Details calendar padding; tiles 6 pt further from the bar; Sort By is a `Picker` in the menu — stock checkmark column — bound to `$store.ordering`/`$store.direction`, mirrored to the stored preference by `.onChange`.)
- **Decided 2026-09-18:** a relaunch never reopens a card (the editing app-storage key is gone; a blank draft left by a kill is dropped on mount); Tags and Flag sit as their own cards after Repeat, before Organisation, as in the shipping app. Print, Location, and smart-group visibility are deferred.
- Evidence lives under `Evidence/Parity/<row>/` and `Evidence/Parity/survey-*/` as stock/ours pairs; geometry in points from 3× captures, colours as sRGB samples.


Stock: iPhone 17 simulator (A10F654F, iOS 27.0, 402×874 pt), `com.apple.reminders`.
Ours: iPhone 17e simulator (E297C542, iOS 27.0, 390×844 pt), `Reminders.app` from the `Reminders` scheme.
Evidence pairs live under `Evidence/Parity/<row>/{stock,ours}-*.png`; geometry comes from the Xcode device
hierarchy dumps (points), colors from sRGB pixel samples of the screenshots.

Excluded by the brief: the bottom-bar search field and the "+" New Reminder bar item (placement, style, behaviour).

| Row | Stock evidence | Ours evidence | Differences found → fixed | Status |
|---|---|---|---|---|
| home: tiles | home/stock-home.png | home/ours-home-before.png → home/ours-home.png | Tiles 181×80, 8 pt gaps, radius 18, 16 pt under the bar (ours were 173×84, 12 pt gaps, 49 pt under the bar); stock gradients sampled top→bottom: Today (125,195,239)→(102,185,237), Scheduled (241,157,156)→(238,142,140), All (106,106,106)→(80,80,80), Completed (166,174,179)→(155,163,169); glyphs are bare white (ours had a tinted square behind); Today's glyph is a calendar page with the day number (drawn); title is title3 semibold (ours headline). Flagged gradient not sampled yet (stock had nothing flagged) — estimated. | done (Flagged gradient estimated: stock had nothing flagged and the simulator's Details sheet has no Flag row) |
| home: My Lists | home/stock-home.png | home/ours-home.png | Header text resolved secondary (`.primary` inside the header's secondary style) → explicit `Color.primary`; rows 70 pt → 62 pt (vertical padding removed); badge flat color, glyph 16 pt semibold (was gradient, 14 pt bold); badge→title 16 pt, count→chevron 10 pt, chevron body semibold. Add List bar glyph composed (page + plus badge; no SF Symbol). | done |
| home: Tags | — (tags need an iCloud account; the simulator's stock app has no Tags row, and `#word` typed into a title stays text) | home/ours-tags.png | Rebuilt as the iOS Reminders tag card: one card of wrapping capsules, "All Tags" first then each `#tag` (body text on tertiary fill, 12/7 pt padding, 8 pt gaps), long-press → Delete Tag; the row list is gone. Geometry from the shipping app's known design, not a simulator capture. | done (stock capture blocked: tags need iCloud, which I cannot sign into) |
| list detail: title | list-detail/stock-personal.png | list-detail/ours-personal-before.png → list-detail/ours-personal.png | Large blue title 34 pt bold at x 16, first row 106 pt under the safe area: identical after fixing the title row to 52 pt (min row height 42 had shrunk it). | done |
| list detail: rows | list-detail/stock-personal.png | list-detail/ours-personal.png | Stock: no separators, 63 pt rows with a gray line (42 pt title-only), circle centred 29 pt from the edge and 10 pt under the row top, title at x 54, subtitle 24 pt under the title top, `!!! ` in the list color. Ours had separators, 74 pt rows, subtitle 22 pt down, 46 pt title-only rows (the title2 circle glyph was 26 pt tall). Fixed: separators hidden, row insets 10/16, min row height 42, VStack spacing 4, circle frame 26×20. Circle stroke color identical (209,209,214 vs 208,208,209). | done |
| list detail: sort menu | list-menu/stock-more.png, stock-sort.png, stock-sort-{manual,deadline,creation,priority,priority-lowest,title}.png | list-menu/ours-more.png, ours-sort.png, ours-sort-deadline-latest.png | Stock More menu: Show List Info, Select Reminders, Sort By (subtitle = current ordering, chevron; items Manual/Deadline/Creation Date/Priority/Title without glyphs, checkmark on the current), Show Completed, Print, Delete List (red). Ours had only Sort By (glyph per item, "Due Date") and Show Completed. Added Show List Info, Select Reminders (edit mode), Delete List; items now glyph-less with a checkmark; "Deadline". Print is out of scope. Creation Date added 2026-09-16 (`Reminder.created`, `Ordering.creationDate`), verified against stock 2026-09-18: stock is oldest first, a new row lands at the bottom — ours too. **Direction added 2026-09-18:** stock's submenu carries a second section for every ordering but Manual — Earliest/Latest First (Deadline), Oldest/Newest First (Creation Date), Highest/Lowest First (Priority), Ascending/Descending (Title); choosing an ordering resets it to the first; rows without a date or priority stay last in both directions (measured: one dated row stays first under Latest First, `!!!` stays first under Lowest First). Ours: `Preference.direction: SortOrder` (a `direction` column, `update.turn(filter, .reverse)`), the SQL turns the key and keeps `nulls: .last`, ties keep the manual position; wording per key in the view. Remaining: stock aligns every title in a checkmark column (UIMenu single selection) while ours indents only the checked row — a SwiftUI `Picker` would give that but needs a binding onto the `@Fetch` preference (`Binding(get:set:)` is ruled out; an opacity-0 checkmark is dropped by the UIMenu bridge, tried). | done (title alignment: open — options: accept; or a stored state mirror bound by `$store` — a parallel structure) |
| list detail: show/hide completed | completion/stock-seq-*.png, list-menu/stock-more.png | completion/ours-header.png, ours-seq-*.png | Stock shows "N Completed • Clear" over a rule when completed are shown (Clear tinted when N > 0); ours had no header. Added (Clear deletes the filter's done reminders; new `deleteCompleted(in:today:)` + feature action, tested). | done |
| inline edit card (chips, Return-continues, Done) | inline-edit/stock-card-new.png | inline-edit/ours-card-new-before.png | Card 107 pt (ours 120 → 112), circle centred 29 pt (ours 31 → 29), chips at x 54 (ours 56 → 54), (i) 26 pt from the trailing edge (ours 28 → 26). Date chip menu items now carry the stock `N.calendar` day glyphs (SF Symbols 7). Stock reverses the menu's item order when it opens upward (Custom at the top, None at the bottom); SwiftUI `Menu` keeps declaration order — recorded, not fixable in SwiftUI. Return-continues and Done were already verified 2026-09-15 morning. | done (menu order: blocked, SwiftUI Menu does not flip for upward presentation) |
| Details sheet | details-sheet/stock-details-top.png, -full.png, -date-on.png | details-sheet/ours-details-before.png → ours-details.png | Stock: opens at content height (624 of 874 pt) and grows to full on a drag, no grabber, opaque grouped background, dimmed behind; sections Date & Time / Organisation (List card, then Priority card) / Places & People (Location); List row shows the badge once and the list name as the value; the card sits 22 pt under the bar. Ours had a large-only sheet, one Organisation card holding Priority/Tags/Flag/Location, a badge repeated in the List value, and a 49 pt gap. Fixed all of these (Tags and Flag kept as one card after Priority — the simulator's stock has no such rows). Remaining: SwiftUI presents a partial detent as the iOS 26 floating inset sheet (0.96 scale, side margins) while stock is edge-attached. | done (partial-detent inset: blocked, no SwiftUI API for an edge-attached partial sheet) |
| New Reminder sheet | new-reminder-sheet/stock-new.png | new-reminder-sheet/ours-new-before.png → ours-new.png | Structure already matched (title/notes card, Date & Time, More Options: List + Details, disabled Done, X). Missing the quick bar above the keyboard (Date & Time menu, Location menu, Flag, Photos disabled): added as a `safeAreaBar` shown while a field has the keyboard — a `.keyboard` toolbar placement never appeared inside this sheet. | done |
| List Info / New List sheet | list-info/stock-info.png, stock-new.png | list-info/ours-info.png | Stock: 100 pt glowing badge, name in the list color on a gray field, seven flat 40 pt color circles six to a row with a gray ring on the current one, no custom-color row, card 22 pt under the bar. Ours had a 96 pt badge, gradient circles with an inner ring, and a ColorPicker row. Fixed. Remaining: our default list blue is the Organizing default (74,153,239) where stock uses system blue (0,122,255). | done (blue: fixed 2026-09-18 — `Color.default` is the system blue (0,122,255), sampled from the stock title and badge; the palette's "Blue" and the seeded lists follow) |
| tag picker | — | — | No stock reference (tags need iCloud; the simulator's Details sheet has no Tags row). Ours unchanged. | blocked (no stock reference) |
| search results (field excluded) | search/stock-results.png, stock-results-te.png | search/ours-results.png | Stock: plain white page, "N Completed • Clear … Show" header at x 16 over a rule, list names as title2 headers in the list color at x 16, 40 pt rows without separators. Ours sat on the grouped gray background with the header indented. Fixed: scroll background hidden while searching, section margins 0, row insets 16, separators hidden. | done |
| swipe actions | swipe/stock-row.png | swipe/ours-row.png | Identical: the row lifts into a capsule, Details (gray, info glyph) then Delete (red, trash) with captions — system-drawn on both. | done |
| Edit-mode reorder | edit-mode/stock-home.png, stock-select.png | edit-mode/ours-home.png, ours-select.png | Home: stock Edit shows the smart groups as rows with grips and visibility checkmarks, lists with delete/(i)/grip, Add Group + Add List, Done as the prominent checkmark; ours had the text EditButton and kept the tiles. Now: Edit text → Done checkmark, smart groups as badge rows with grips, (i) replaces the count on list rows. Detail: stock "Select Reminders" title, grips, no + and no More; ours matches (selection circles are still the completion circles). Visibility toggles and Add Group are not modelled. | done (smart-group visibility: blocked, no domain rule) |
| relaunch while editing | relaunch-editing/stock-before-kill.png, stock-after-relaunch.png, stock-after-background-relaunch.png | relaunch-editing/ours-after-background-relaunch.png, ours-before-hard-kill.png, ours-after-hard-kill.png | Stock never reopens an editing row: backgrounding commits the draft ("Draft two" beneath Hallo) and the relaunch shows the list plainly; a hard kill mid-edit drops the draft (no row at all). Ours on the background path is the same (the draft is committed, no card on relaunch, the row at its sort position — where a committed undated row sits in stock too). Ours after a hard kill reopens a blank card at the sort position (the draft text is not persisted per keystroke). | product decision — options: (a) match stock: drop the `remindersEditing` restore, a launch never reopens a card (removes app-storage state and one test); (b) keep the restore as is (a blank card, more than stock offers); (c) keep it and persist the draft per keystroke (more writes). Recommendation: (a). |
| Return continues beneath: keyboard | (stock keeps the keyboard up and every keystroke after Return lands in the new row — a synthesized "⏎Draft" typed "Draft") | return-keyboard/ours-return.mp4.strip.png (before), return-keyboard/ours-return-stable.mp4.strip.png (after) | Before: after Return the old card left the list, the keyboard went down and came back (~0.5 s), and every key typed meanwhile was dropped (0 of 10 in a synthesized burst); the focus was set by a 120 ms retry loop. Now: Return moves the editing onto the next row synchronously (the placement is a sync read, the row is written in the task), the listing's `contents` shows the pending row beneath its anchor until the page carries it, and the editing card keeps one identity in the `ForEach` (`Reminder.Key.editor`) so the same field survives the move; the retry loop is gone. 8 of 10 keys land; the keyboard still dips and returns because SwiftUI's `TextField` resigns on submit unless `onSubmit` moves focus to a *different* existing field (the only other field is the notes field, and focusing it names a blank row "New Reminder" as stock does). | done (residual: the keyboard dip on Return is a SwiftUI `TextField` submit behaviour; the two keys typed inside the Return handling are lost) |
| smart lists: All / Flagged / tags | smart-lists/stock-all.png, stock-scheduled.png, stock-today.png | smart-lists/ours-all.png | Stock All gathers the rows list by list: a title2 header in the list's color at x 16, 64 pt under the title, then the list's rows, then a dotted circle to add a row to that list; no + bar item. Ours was one flat list by the smart list's ordering. Now `Filter.groupsByList` (All, Flagged, tags) orders by the lists' positions first and the view sections the runs under the search's header (title2 bold, list color). Not modelled: the per-section dotted add circle; Completed's grouping (not measured yet). Stock Scheduled sections by date (Overdue with day subheaders, Today with an add circle, Tomorrow, the next five weekdays, Rest of <month>, then months) and its rows show the list name instead of the date; stock Today shows overdue rows first, then Morning / Afternoon / Tonight, rows subtitled "<list>  <date>". Ours keep one flat list for both. | done (Scheduled/Today date sections: product decision — options: (a) model `Reminders.Read.Page` sections by due day on the SQL side and render the stock headers, rows subtitled with the list; (b) keep flat. The per-section add circle needs a "new row in list X from a smart list" action) |
| smart lists: rows name their list | smart-lists/stock-today.png, stock-scheduled.png, stock-completed.png | smart-lists/ours-today.png | Stock rows in Today, Scheduled, and Completed carry the list's name at the start of the subtitle ("Reminders  16/09/2026"); ours showed the date alone. Now the row takes the list's name on every screen that gathers rows from all lists without a list header. Stock Completed sections by completion time (Today, Previous 7 Days with day subheaders) and adds "Completed: <when>" — we keep no completion time. | done (Completed sections: product decision — needs a completion timestamp on the domain (`completed: Date?` in place of the flag), a column, and date sections) |
| completion circle + grace | completion/stock-seq-01…16.png (0.2 s cadence) | completion/ours-seq-01…13.png | Circle stroke identical (26 pt glyph, 209/208 gray, centred 29 pt from the edge); filled circle in the list color. Timing with completed shown: stock fills the circle at the tap and the row moves into the completed group ~0.4 s later (≈0.5 s move); ours moves at once. With completed hidden both keep the row in place through the grace period (ours 5 s). A version that held the row in place while shown was tried and reverted after the stock frames showed the immediate move. | done |
| row insert/delete/move animations | completion/stock-seq-*.png (the completed row's move), list-detail | ours-seq-*.png | Row moves animate with the List's default (`.animation(.default, value: ids)`), matching the stock ≈0.5 s slide; Return-continues inserts and swipe deletes use the same List animations. No separate frame sheet beyond the completion move. | done |
| sheet present/dismiss | stock-nav.mp4 frames 864–879 present (0.5 s), 949–960 dismiss (0.4 s) | ours-nav.mp4 frames 90–107 present (0.57 s), 169–180 dismiss (0.4 s) | Same UIKit sheet transition (slide up, presenter dims and scales). Ours shows the keyboard rising a beat after the sheet; stock's quick bar rides the sheet as an input accessory. | done |
| push/pop | stock-nav.mp4 frames 702–716 push (0.5 s), 776–787 pop (0.4 s) | ours-nav.mp4 frames 0–11 (push tail) | Standard navigation push with parallax on both; stock highlights the tapped list row during the push, ours does not. | done (row highlight: open, minor) |
| empty states | empty/stock-list.png | empty/ours-list.png | Stock centres "No Reminders" (title3, tertiary) in an empty list; ours had nothing. Added. | done |
| Dynamic Type ×2 | dynamic-type/stock-home-dark.png (XXXL) | dynamic-type/ours-home-dark.png | Tiles grow to 88 pt on both, titles and rows scale alike; list rows stay 62 pt on both. | done |
| dark mode | dark/stock-empty-list.png, dynamic-type/stock-home-dark.png | dark/ours-empty-list.png, dynamic-type/ours-home-dark.png | Tile gradients are identical in dark (sampled). Stock's lists card is (37,37,40) where ours is the system (28,28,30). | done (card gray: open, minor) |

## Log

- 2026-09-15 22:38 baseline: `reminders.xcworkspace` scheme 67/67 green on main d325289. Stock app launched on the iPhone 17; a "You won't be notified" card was dismissed before capture.
- 2026-09-15 22:47 home tiles + My Lists restyled; `listSectionMargins(.top, 0)` closes the gap under the bar.
- 2026-09-15 22:50 stock content added on the iPhone 17 (Personal: Pay rent !!! Today, Dentist appointment, Book flights to Lisbon Tomorrow) for the list-detail, card, and Details comparisons.
- 2026-09-15 23:06 functional defect found while re-launching ours onto an open list: the detail stayed empty (filter restored from a mount task after the detail's first read). Fixed by restoring the session synchronously in `onMount`; new test `a relaunch onto an open list reads its rows` — 68/68.
- 2026-09-15 23:14 Details sheet regrouped + partial detent; New Reminder quick bar; list rows/card geometry. 68/68 (run by the principal in the GUI).
- Tooling: `DeviceInteractionSynthesize` screenshots are 1× — evidence is captured with `simctl io screenshot` (3×). Only one device-interaction session exists at a time (starting a second ends the first; `RunProject` also ends it), and session names cannot be reused.
- 2026-09-16 06:23 tag cloud, More menu, completed header, Select mode, empty state, home edit mode, List Info palette, search page; stock survey of swipe/select/edit/list info/empty/dark/XXXL and navigation videos. 68/68. A device-session approval sat unanswered from ~00:00 to 06:17 (each new Xcode device session prompts; `simctl io recordVideo` and `RunProject` end the session, so the loop now builds with BuildProject and relaunches with `simctl install/launch` to keep one session alive).

## Remaining differences (top three) and proposed fixes

1. ~~Sort By lacks Creation Date~~ — implemented 2026-09-16 (see the sort-menu row).
2. **Partial-detent Details sheet floats inset.** SwiftUI presents a non-large detent as the iOS 26 floating sheet (0.96 scale, side margins) while stock is edge-attached at content height. No SwiftUI API today; revisit if `presentationSizing` gains an iPhone form, otherwise keep the detent (height and dimming match).
3. ~~Default list blue~~ — fixed 2026-09-18 (`Color.default` = system blue).

Also open, minor: the chip menu keeps declaration order when it opens upward (stock flips it); the tapped list row is not highlighted during the push; the dark lists card is (28,28,30) vs stock (37,37,40); select mode keeps the completion circles where stock shows selection circles; smart-group visibility toggles and Add Group in home edit mode are not modelled.
- 2026-09-18 00:46 branch feature/stock-reminders-parity: ledger restored from history (it left with 7a5282d); sort direction landed, 87/88 (the one red is the accepted upstream TCA26 cached-state case). Stock and ours now share the iPhone 17e simulator.
- 2026-09-16 06:38 Creation Date ordering implemented end to end; device-verified on a migrated database (menu shows Manual / Deadline / Creation Date / Priority / Title; earlier rows keep the manual order). 68/68.

## Survey 2026-09-18

Fresh side-by-side pass on the iPhone 17e simulator (390×844 pt, iOS 27.0), stock `com.apple.reminders` against `Reminders.app` built from `reminders.xcworkspace` on `feature/stock-reminders-parity` (93831ed). Ours seeded with the DEBUG leaf menu's Reference sample; stock given matching content by hand (lists Reminders/Family/Business, rows Haircut 16/09 overdue, Draft two today 15:00, Third today, Pick up kids tomorrow 15:00 `!!!`, Take out trash + Call accountant + Prepare for WWDC 21/09, three completed). Evidence under `Evidence/Parity/survey-*/`. Geometry in points from the 3× captures (px ÷ 3); colours are sRGB pixel samples. The bottom-bar search field and the "+" bar item stay excluded. Tags, Flag, and Location have no stock reference on this simulator (no iCloud) and are not listed.

Screens covered: home, Today, Scheduled, All, Completed, list detail (rows, completed header, More menu, swipe, inline card, all three chip menus, Custom sheet, new-card placement, Details from a blank draft), Details sheet (date on, calendar, priority menu), New List, dark mode. Push/pop, sheet timing, Dynamic Type, edit mode, search, List Info were re-checked against the earlier rows and unchanged — not re-measured.

Ranked by how often a user sees it. Effort S = an hour, M = a session, L = more than one session.

| # | Screen | Stock (measured) | Ours | Proposed shape | Evidence | Effort | Decision? |
|---|---|---|---|---|---|---|---|
| 1 | Today smart list | Overdue rows are included: tile count 3 with one overdue row; the list shows overdue + all-day rows first (no header), then Morning / Afternoon / Tonight sections split by 2 pt rules, empty sections keep a dimmed header, the current section carries a dotted add circle. Row subtitle is `<list>  <time>` (no "Today"); overdue subtitle `<list>  16/09/2026` in red. | Tile 2, overdue row absent; flat list; subtitle `Personal  Today, 6:05`. | Domain: `isDue(during: today)` → `!isCompleted && dueDate < today.upperBound` for the `.today` filter and the tile count (one predicate in `Reminder.Record.TableColumns`). Sections: `Reminders.Page` rows grouped by a `Reminders.Today.Part` (overdue, allDay, morning, afternoon, tonight) computed in SQL from the due hour, rendered as `Section`s under the existing list-header style; the add circle is the per-section "new row" action already listed under the smart-lists row. Subtitle: the row's due presentation drops the day word when the screen is Today. | survey-smart-lists/stock-today.png, ours-today.png; survey-home/stock-home.png (tile 3) | M (predicate + subtitle S; sections M) | yes — sections (b) vs flat; the overdue predicate is a plain bug and needs no decision |
| 2 | Scheduled smart list | Sections: **Overdue** (title2 bold, primary) with `Wed 16 Sep` day sub-headers (weekday semibold + date, secondary), **Today** (title2 bold) + dotted add circle, **Tomorrow** (headline, secondary) + add circle, then the next five days as `Sun 20 Sep` sub-headers (empty ones dimmed, dotted rules), **Rest of September**, then every month for twelve months (title2 bold, tertiary, empty with 2 pt rules). The current day header pins under the bar while scrolling. Rows: subtitle `<list>  <time>`; `!!!` tinted with the smart list's red (255,56,60), not the list colour. | Flat list, subtitle `<list>  Sunday, 6:05`, `!!!` in the list colour. | Domain: a `Reminders.Schedule.Section` enumeration (overdue day, today, tomorrow, next five days, rest of month, following months) generated from `now` + calendar, rows bucketed by due day in SQL (`ORDER BY date(dueDate)` with the section key selected), empty sections come from the enumeration not the data. View: `Section` per key with `.listSectionMargins`, pinned headers from `List`'s default. Priority tint: the row takes the *screen's* tint (`Reminders.Filter.Style.tint`) for `!!!` on smart lists. | survey-smart-lists/stock-scheduled.png, stock-scheduled-2.png, ours-scheduled.png | L | yes — same product decision as row 1 (b); priority tint is S and decision-free |
| 3 | Details sheet height | Edge-attached sheet, top at 46 pt on both a full reminder (Date/Time/Repeat/List/Priority/Location) and a blank draft — it is a large detent whose content sits at the top; no floating inset. | `.presentationDetents([.fraction(0.715), .large])` → iOS 26 floating inset at 278 pt, Priority/Tags/Flag hidden until dragged. | Drop the fraction: `.presentationDetents([.large])` in `Reminders.Screen` (the ledger's earlier 624-of-874 measurement was content height on a shorter sheet; today's stock is full-height in both cases). | survey-details/stock-details-dated.png, stock-details-blank-draft.png, ours-details-dated.png | S | no |
| 4 | Details sheet card spacing | Date & Time card → Repeat card gap 9 pt; List card → Priority card gap 10 pt (separate cards, compact spacing). | 36 pt between the same cards (each is a `Section` with default spacing). | `.listSectionSpacing(.compact)`/`(10)` on the form list, or keep Repeat inside the Date & Time section as its own row group — matches stock's grouping without a wrapper. | survey-details/stock-details-dated.png, ours-details-dated.png | S | no |
| 5 | Details calendar padding | Rule under the Date row → month header 35 pt; last week row → rule above Time 40 pt. | 73 pt and 70 pt: the graphical `DatePicker` row keeps the list's row insets. | `.listRowInsets(EdgeInsets())` on the picker row (the picker draws its own margins). | survey-details/stock-details-dated.png (calendar), ours-details-dated.png | S | no |
| 6 | Inline card: Custom / (i) on a blank draft | Custom on the date or time chip opens a dedicated **Date & Time** sheet (Date toggle + calendar + Time toggle + Repeat) and keeps the draft; (i) on a blank draft names it "New Reminder", opens Details, and X keeps the row. | Both go through `details(id:)` which commits first; a blank draft is deleted by the commit, `retrieve` returns nil, editing ends, no sheet. | Feature: `detailsButtonTapped` on a blank draft names it "New Reminder" (the `notesFocused` rule already does this) before committing; Custom posts a `DateRequested` destination that presents the existing Date & Time rows as their own sheet (`Reminders.Feature.Destination.dates`) instead of the full form. | survey-card/stock-custom-date-sheet.png, stock-details-blank-draft.png; ours: editing ends (no capture possible — the sheet never appears) | M | no (bug) |
| 7 | Date presentation in rows and Details | Rows: `Today`, `Tomorrow, 15:00`, then numeric `21/09/2026` for anything further (no weekday names); Details value: `Tomorrow` / `Wednesday, 16 September 2026`; times zero-padded `06:15`, `15:00`. | Rows: weekday names for 2–6 days (`Sunday, 6:05`), `16 Sep 2026` abbreviated beyond; Details `16 Sep 2026`; time `6:05`. | `Reminder.Due.dayDescription`: cases 0/1/−1 keep the words, everything else `date: .numeric`; Details value uses `.complete`; time style `.shortened` → an explicit `hour(.twoDigits(amPM: .omitted))` under a 24-hour locale (stock respects the locale's 12/24 h; the simulator is 24 h). | survey-list-detail/stock-personal.png vs ours-personal.png; survey-smart-lists/stock-completed.png (06:15) | S | no |
| 8 | Date chip menu: weekend item | `Next Weekend` = the Saturday after this coming one when today is Friday (26); items Today/Tomorrow/Next Weekend/Next Week/Custom. Also: the menu keeps None-first order when it opens upward with the keyboard up — the earlier "stock flips the order" note is withdrawn. | `This Weekend` = tomorrow (19), duplicating Tomorrow. | `Preset.thisWeekend` → `.nextWeekend`: first Saturday strictly after tomorrow; label "Next Weekend". Remove the "menu order: blocked" residue from the inline-edit row. | survey-card/stock-date-chip-menu.png, stock-date-chip-menu-upward-keyboard.png, ours-date-chip-menu.png | S | no |
| 9 | Completed smart list | Title and completed circles in the smart list's slate (88,103,113); sections **Today** / **Previous 7 Days** with `Tue 15 Sep` sub-headers; third subtitle line `Completed: Today, 06:15`; header `5 Completed • Clear` without a rule. | Title system gray (142,142,147), circles in each list's colour, flat, no completion time, rule under the header. | Colour: `Filter.Style.tint` for `.completed` = (88,103,113) and the row's circle takes the screen tint on smart lists (same change as row 2's `!!!`). Sections + "Completed:" line need `completed: Date?` on the domain (already listed as a product decision). | survey-smart-lists/stock-completed.png, ours-completed.png | S (colours) / M (timestamp + sections) | timestamp: yes (already open); colours: no |
| 10 | Notes in rows | Notes keep their line breaks (a two-line note shows two lines, blank line preserved), up to the row's line limit. | `replacingOccurrences(of: "\n", with: " ")` then `lineLimit(2)`. | Drop the replacement; keep `lineLimit(2)` (stock also truncates). | survey-smart-lists/stock-completed.png ("note line" row); survey-smart-lists/ours-scheduled.png (Call accountant) | S | no |
| 11 | New card placement with completed shown | The editing card is appended after the completed rows; on Done the saved row settles above the completed group. | Card inserted above the completed group. | `startNewReminder` placement `.below(lastShown)` when completed are shown; the commit already re-sorts. | survey-card/stock-card-new-after-completed.png; survey-card/ours-card-new.png | S | minor — accept or match |
| 12 | Completed header geometry | Header baseline 178 pt, rule 202 pt, first row title 222 pt. | 173 / 197 / 228. | Header row insets +5 pt top, first row inset −6 pt (`Reminders.Listing.SwiftUI` header `listRowInsets`). | survey-list-detail/stock-personal-completed-shown.png, ours-personal-completed-shown.png | S | no |
| 13 | Swipe "Details" glyph | Plain `info` glyph (an "i" without a circle) in the gray capsule. | `info.circle.fill`-style glyph (an "i" inside a small circle). | `Label("Details", systemImage: "info")`. | survey-list-detail/stock-swipe.png; ours: the swipe on Haircut in this session (system-drawn capsule, glyph differs) | S | no |
| 14 | Header separator | `N Completed • Clear` uses a bullet (U+2022). | Middle dot (U+00B7). | Change the literal. | same as row 12 | S | no |

Unchanged from the earlier rows and still open: Sort By checkmark column alignment; Creation Date verified; row highlight during push; dark lists card gray; select-mode circles; smart-group visibility/Add Group; relaunch-while-editing product decision.

Withdrawn today: "stock reverses the chip menu when it opens upward" (it does not — see row 8); "Details opens at content height" (it is a large detent — see row 3).

### S-batch landed (2026-09-18, after the survey)

Rows 1 (predicate only), 3, 4, 5, 7, 8, 10 implemented; 87/88 (the one red stays the accepted upstream TCA26 cached-state case). After-pairs: `survey-list-detail/ours-personal-after.png` (16/09/2026, 06:05, notes on their own lines, Today tile 3), `survey-details/ours-details-dated-after.png` (large sheet, 10 pt card gaps, "Wednesday, 16 September 2026"), `survey-details/ours-details-calendar-after.png` (rule → month header 47 pt vs stock 35 — the picker's own top padding; horizontal inset kept at 22 pt). Rows 2, 6, 9, 11–14 remain open; rows 1b/2/9 sections ruled yes 2026-09-18 and follow in their own commits.

### Smart-list sections landed (2026-09-18, ruled)

Rows 1b, 2, and 9 implemented domain-first: `Reminders.Section` (Sources/Reminders) enumerates the stock sections per filter and classifies a row by its due or completion date; `Reminders.Page` folds the ordered rows into `sections` in one pass inside the read request (search pages fold by list the same way, `Search.Contents` maps them); the listing view renders `ForEach(contents.sections)`. `completed` is a `Date?` now (migration "Completion is a time": earlier completed rows take the migration's moment; partial index on `completed IS NOT NULL`), the Completed screen runs newest first with Today / Previous 7 Days / month sections and a `Completed: <when>` line. Measured against the stock captures: Overdue header 430 vs 425 px, day line 494 vs 485, first row 566 vs 560, rule → Today header 28 pt vs 26 pt; a section's last row → rule 22 pt vs 11 pt (residual). Findings that differ from the ruling's wording, taken from the captures: the Today add circle sits in the last part of the day that has rows (stock had it under Afternoon at 06:21), not the part containing now; Scheduled's circle only under Today and Tomorrow; part headers (Morning/Afternoon/Tonight) are headline secondary, Overdue/Today title2 primary. After-pairs: `survey-smart-lists/ours-today-after.png`, `ours-scheduled-after.png`, `ours-completed-after.png`. 92/93 (the accepted upstream TCA26 case).

### Survey rows 6, 11–14 closed; rows drag onto sections (2026-09-18)

- Row 6: Custom on a blank card names it "New Reminder", dates it today, and opens a **Date & Time** sheet (the same `Reminder.Form` with `part: .dates`, calendar open); (i) names it and opens Details; X keeps the row (`survey-card/ours-custom-date-sheet-after.png` vs `stock-custom-date-sheet.png`).
- Row 11: the new card sits under the completed rows while it is being typed (`Page.moveToEnd`), the saved row settles above them.
- Rows 12–14: completed header 15/6 pt insets, footnote bullet, swipe glyph `info`.
- **Drag onto a section:** the listing is one flat reorderable run (headers, rows, add circles, landing rows, rules are all items of one `ForEach`), so a long-press drag on Today/Scheduled lands a row in another section; the drop reads the section under the landing point and sends `reminderDropped(id, into:)` → `Reminders.Section.due(moving:at:calendar:)` (a day keeps the row's time of day, a part of the day sets its hour; overdue/months/lists take no drops). Verified on the simulator: Doctor appointment 10:40 → Afternoon → 12:00 (`survey-smart-lists/ours-today-dragged-to-afternoon.png`). Cost: the day headers no longer pin under the bar while scrolling (they are rows now). Tried and rejected: `.draggable`/`.dropDestination`, `.onDrag`/`.onDrop`, `.onInsert` — none receive the drop inside a `List` whose rows lift as a reorder; `.moveDisabled` on the header items also blocks the landing.

### Recently Deleted (2026-09-18)

The simulator's stock app has no Recently Deleted list (it needs iCloud reminders): deleting a row or a list there drops it at once (`recently-deleted/stock-delete-list-alert.png`). Built from the shipping design instead: a delete stamps `Reminder.deleted` and the row waits thirty days (purged on activation); Recently Deleted closes My Lists while it holds anything, with a gray trash badge and the count; in Edit mode it has no delete control, no (i), and no grip; the screen is gray-titled, notes the thirty days, lists newest deletion first with the list name, and a swipe offers Recover or Delete (for good); the More menu keeps only Select Reminders; blank drafts are still dropped outright; every other screen, count, and search reads the kept rows only. Evidence: `recently-deleted/ours-home.png`, `ours-home-edit.png`, `ours-list.png`, `ours-swipe.png`. Deleting a list stamps the list and its reminders together: they wait in Recently Deleted (rows still name the list, from `Summary.trash`), recovering any reminder brings the list back at its position, and the purge drops both after thirty days (`recently-deleted/ours-list-after-list-delete.png`).

// Aspirational syntax; see FEATURE-SYNTAX.md. Bridge support is not implemented.

// No declaration required. Interface owns call optics; the bridge consumes them
// directly without requiring consumers to add CasePathable conformances.
